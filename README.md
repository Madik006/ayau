# Romantic Letter Website

This folder contains a small romantic landing page designed for Ayaulym.

## Files
- `index.html` — the interactive letter with wax seal animation.
- `reflections.mp3` — optional audio file to play "The Neighbourhood – Reflections" if you add it.

## How to use
1. Open `index.html` in a browser.
2. Click the wax seal to open the letter and trigger the flower animation.
3. To play music, place the song file named `reflections.mp3` next to `index.html`.

## Upload to GitHub
1. Create a new repository on GitHub.
2. Initialize git in this folder:
   ```bash
   cd "c:\Users\madiy\Downloads\love-letter-site"
   git init
   git add index.html README.md
   git commit -m "Add romantic letter site"
   git remote add origin <your-github-repo-url>
   git push -u origin main
   ```
3. If you want, add the `reflections.mp3` file later and push again.

> Note: I cannot upload to GitHub directly from here, but the code and git repo are ready for you to push.
